#include <bits/stdc++.h>
using namespace std;

#define FastIO ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)

int main() {
	FastIO;
	int x = 2, y = 0;
	// y = ++x;
	cout << y << '\n';
	y = (x) + (x) + (x);
	cout << y << '\n';
	return 0;
}