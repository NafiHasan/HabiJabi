#include <bits/stdc++.h>
using namespace std;

#define fast_io() ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)

int main() {
	fast_io();
	while (1) {
		while (1) {
			string s;
			cin >> s;
			if(feof(stdin))return 0;
			
			for (int i=0; i<n; i++){
				
			}
		}

	}
	return 0;
}