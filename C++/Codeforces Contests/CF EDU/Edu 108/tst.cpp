#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
#define en "\n"
#define fast_io() ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)
#define w(t) int t; cin >> t; while (t--)

int main() {
	fast_io();
	int n = 200000;
	cout << n << en;
	for (int i = 0; i < n; i++)cout << 1 << " ";
	cout << en;
	for (int i = 0; i < n; i++)cout << 500 << " ";
	cout << en;
	return 0;
}