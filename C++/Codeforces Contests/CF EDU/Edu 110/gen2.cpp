#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
#define en "\n"
#define fast_io() ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)
#define w(t) int t; cin >> t; while (t--)

int main() {
	fast_io();
	int a;
	float x, y;
	x = 100.1415;
	y = 3.97;
	a = x + y;
	cout << a << en;
	// cout << 18 << en;
	// for (int i = 1; i < (1 << 18); i++)cout << "1";
	// cout << en << 1 << en;
	// cout << 4 << " ?\n";
	return 0;
}