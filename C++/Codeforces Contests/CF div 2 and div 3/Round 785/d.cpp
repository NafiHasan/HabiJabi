#include <bits/stdc++.h>
using namespace std;

#define fast_io() ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)
#define ll long long

ll mod = 1e9 + 7;

int main() {
	fast_io();
	int tc;
	cin >> tc;
	while (tc--) {
		ll a, b, c, d, e , f;
		cin >> a >> b >> c >> d >> e >> f;

	}
	return 0;
}