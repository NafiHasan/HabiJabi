#include <bits/stdc++.h>   
using namespace std;
 
#define fast_io() ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)

int main()
{
	fast_io();
 	int  tc;
 	cin >> tc;
 	while(tc--){
 		int n;
 		cin >> n;
 		for (int i=0; i<n; i++){
 			for (int j=0; j<n; j++){
 				for (int k =0; k <n; k++){
 					int x;
 					cin >> x;
 				}
 			}
 		}
 		for (int i=0; i<n; i++){
 			vector<int>v(n);
 			vector<int>v2(n);
 			for (int i=0; i<n; i++)cin >> v[i];

 		}
 	}
    return 0;
}